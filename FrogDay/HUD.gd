extends CanvasLayer

func ready():
	$Message.hide()

## Below code was used for bug testing by displaying internal values
#func update_chirp(lastChirp):
#	$Chirp.text = str(lastChirp)
#func update_score(score):
#	$Score.text = str(score)
